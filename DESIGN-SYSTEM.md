# Bio-Graphene — نظام التصميم (Design System)

مرجع موحّد لألوان المشروع وخطوطه ومسافاته ومكوّناته. كل القيم مأخوذة من المتغيّرات في
[`public/css/site.css`](public/css/site.css) (`:root`). عدّل القيمة في مكان واحد (`:root`) لتنعكس على الموقع كله.

> **الطابع العام:** واجهة داكنة (Dark UI) · اتجاه RTL (عربي) · لمسات نيون خضراء/سماوية متدرّجة · زوايا دائرية كبيرة.

---

## 1) لوحة الألوان (Color Palette)

### الخلفيات (Backgrounds)
| الدور | المتغيّر | القيمة | معاينة |
|---|---|---|---|
| الخلفية الأساسية | `--bg` | `#020c1d` | كحلي شبه أسود |
| خلفية ثانوية / الفوتر | `--bg2` | `#06142b` | كحلي أغمق قليلًا |
| سطح البطاقات | `--surface` | `#0a1a30` | كحلي للبطاقات |
| سطح عند الـ hover | `--surface2` | `#0e2238` | أفتح شوية للتفاعل |

### ألوان العلامة (Brand / Accent)
| الدور | المتغيّر | القيمة | الاستخدام |
|---|---|---|---|
| الأخضر الأساسي | `--green` | `#1FD27A` | النقاط، الظلال، الحدود النشطة |
| الأخضر الفاتح | `--green-b` | `#22E08A` | الأيقونات، الروابط، الـ eyebrow |
| السماوي | `--cyan` | `#19C5E0` | روابط، نهاية التدرّج |

### التدرّجات (Gradients)
| المتغيّر | القيمة |
|---|---|
| `--grad` | `linear-gradient(120deg, #22E08A 0%, #19C5E0 100%)` |
| `--grad-soft` | `linear-gradient(120deg, rgba(34,224,138,.16), rgba(25,197,224,.16))` |

> `--grad` للأزرار والنصوص المتدرّجة (class `.grad-text`) · `--grad-soft` للخلفيات الخفيفة (eyebrow، CTA، البنرات).

### النصوص (Text)
| الدور | المتغيّر | القيمة |
|---|---|---|
| نص أساسي | `--text` | `#E6EDF6` |
| نص ثانوي | `--muted` | `#9FB0C4` |
| نص خافت | `--faint` | `#64768c` |
| نص على زر متدرّج | — | `#042418` (كحلي غامق على الأخضر) |
| نص متن المقالات | — | `#cdd9e7` |

### الحدود والفواصل (Lines)
| الدور | المتغيّر | القيمة |
|---|---|---|
| حد عادي | `--line` | `rgba(255,255,255,.08)` |
| حد بلون العلامة | `--line-acc` | `rgba(34,224,138,.28)` |

---

## 2) الخطوط (Typography)

ثلاث عائلات (مُحمّلة من Google Fonts في [`layouts/public.blade.php`](resources/views/layouts/public.blade.php)):

| المتغيّر | الخط | الاستخدام | الأوزان |
|---|---|---|---|
| `--ar` | **Cairo** | العناوين العربية (h1/h2/h3) | 400 · 600 · 700 · 800 · 900 |
| `--arb` | **Tajawal** | نص الجسم العربي (body) | 400 · 500 · 700 |
| `--lat` | **Sora** | اللاتيني/الأرقام/الرموز (Bio-Graphene، Au، 100%) | 400 · 600 · 700 · 800 |

**الأوزان المستخدمة:** `600` (متوسط) · `700` (عريض) · `800` (أعرض/العناوين).

### مقياس الأحجام (Type Scale — متجاوب بـ `clamp`)
| العنصر | الحجم |
|---|---|
| عنوان الهيرو `h1` | `clamp(2.1rem, 5vw, 3.6rem)` |
| عنوان قسم `h2.sect-title` | `clamp(1.9rem, 4.4vw, 3rem)` |
| عنوان صفحة/مقال `h1` | `clamp(2rem, 5vw, 3rem)` |
| اقتباس الـ mission | `clamp(1.15rem, 2.4vw, 1.6rem)` |
| العنوان الفرعي للهيرو `.sub` | `clamp(1rem, 2vw, 1.35rem)` |
| قيم الأرقام `.stat .v` | `clamp(2.2rem, 5vw, 3.2rem)` |
| نص الأقسام `.sect-lead` | `1.06rem` |
| متن المقالات `.detail-body` | `1.05rem` |
| الـ eyebrow | `.72rem` (uppercase · letter-spacing .16em) |

> `line-height` الافتراضي للجسم: `1.85` · للعناوين: `1.25`.

---

## 3) المسافات (Spacing — قاعدة 8px Grid)

كل المسافات الهيكلية مضاعفات **8px**. السلّم المستخدم:

```
4 · 8 · 12 · 16 · 24 · 32 · 40 · 48 · 64 · 96 · 128 · 144 · 160
```

| الاستخدام | القيمة |
|---|---|
| فاصل بداية القسم `section` (padding-top فقط) | `96px` (موبايل: `72px`) |
| مسافة فوق الفوتر | `96px` |
| padding البطاقة `.card` | `32px` |
| الفجوة بين البطاقات `.grid gap` | `24px` |
| padding الزر `.btn` | `16px 24px` |
| padding الحاوية `.wrap` | `0 24px` |
| هامش العنوان السفلي | `16px` |

> **قاعدة الأقسام:** المسافة في **بداية** القsection فقط (`padding: 96px 0 0`) — لتجنّب تضاعف الفراغ بين قسمين.

---

## 4) الزوايا والظلال (Radius & Shadows)

### نصف القطر (Border Radius)
| المتغيّر | القيمة | الاستخدام |
|---|---|---|
| `--r` | `16px` | البطاقات |
| `--r-lg` | `22px` | البنرات الكبيرة (mission، CTA، الفورم) |
| — | `12px` | الحقول، الأيقونات الصغيرة، الـ chips |
| — | `999px` | الأزرار والـ eyebrow (كبسولة) |

### الظلال (Shadows)
| الاستخدام | القيمة |
|---|---|
| زر متدرّج (راحة) | `0 8px 30px rgba(31,210,122,.28)` |
| زر متدرّج (hover) | `0 14px 40px rgba(31,210,122,.45)` |
| توهّج النقطة | `0 0 10px var(--green)` |
| توهّج الـ particle | `0 0 18px rgba(34,224,138,.25)` |
| حلقة تركيز الحقل (focus) | `0 0 0 3px rgba(34,224,138,.12)` |

---

## 5) المكوّنات (Components)

| المكوّن | الـ Class | الوصف |
|---|---|---|
| زر متدرّج | `.btn .btn-grad` | الإجراء الأساسي (أخضر→سماوي + ظل) |
| زر شبح | `.btn .btn-ghost` | إجراء ثانوي (حد فقط) |
| eyebrow | `.eyebrow` | شارة صغيرة فوق العناوين (كبسولة + نقطة) |
| نص متدرّج | `.grad-text` | تلوين الكلمات بالتدرّج (مثل «معنا»، Mining Earth) |
| بطاقة | `.card` | حاوية محتوى (سطح + حد + شريط علوي متدرّج عند hover) |
| بطاقة تطبيق | `.card.app` | بطاقة بأيقونة سداسية (hex tile) |
| بنر اقتباس | `.mission` | شريط الرسالة بخلفية متدرّجة خفيفة |
| بنر دعوة | `.cta-band` | «تواصل معنا» بخلفية متدرّجة |
| كشف عند التمرير | `.reveal` | يظهر العنصر بحركة لطيفة عند الـ scroll |

### الأبعاد الثابتة
| العنصر | القيمة |
|---|---|
| أقصى عرض للمحتوى `--maxw` | `1200px` |
| ارتفاع شريط التنقّل | `80px` |
| الأيقونة السداسية `.tile` | `58px` |

---

## 6) نقاط التجاوب (Breakpoints)

| النقطة | السلوك |
|---|---|
| `≤ 980px` | عمود واحد للهيرو · قائمة موبايل (burger) · شبكات لعمودين |
| `≤ 640px` | كل الشبكات عمود واحد · تصغير خطوط الهيرو · الفوتر عمودين |

---

## 7) إرشادات الاستخدام

- **الألوان:** استعمل المتغيّرات دائمًا (`var(--green-b)`) بدل القيم الصريحة (`#22E08A`).
- **التدرّج:** للنصوص استخدم `.grad-text` · للخلفيات استخدم `--grad-soft`.
- **المسافات:** التزم بسلّم 8px. القيم الأصغر (4–5px) للعناصر الدقيقة فقط (نقاط/أيقونات).
- **الخطوط:** عربي عناوين → Cairo · عربي جسم → Tajawal · لاتيني/أرقام → Sora.
- **الاتجاه:** الموقع RTL؛ استخدم خصائص منطقية (`margin-inline`, `padding-inline`) عند الإضافة.

> أي لون أو مسافة جديدة؟ أضِفها كمتغيّر في `:root` بـ [`public/css/site.css`](public/css/site.css) ثم وثّقها هنا.
